import 'package:flutter/material.dart';

class AvalaibleProductCard extends StatelessWidget {
  const AvalaibleProductCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
